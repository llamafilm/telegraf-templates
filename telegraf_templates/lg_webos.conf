[[inputs.exec]]
  alias = "{{device.id}}"
  commands = ["python3 /apps/aragorn/exec_scripts/lg_webos.py {{device.primary_ip.address.ip}} {{device.serial}}"]
  timeout = "5s"
  data_format = "influx"
  [inputs.exec.tags]
{% include '100' %}
